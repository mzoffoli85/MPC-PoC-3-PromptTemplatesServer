[project]
name = "poc3-mcp-prompts"
version = "0.1.0"
description = "MCP server que expone Prompts parametrizados (PoC 3 — Aprender MCP en profundidad)"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.28.1",
]
